<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>ProductoParcial2</title>
<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #f0f0f0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
  }
  .container {
    background-color: #d3d3d3;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    text-align: center;
    max-width: 800px;
    width: 100%;
  }
  h1 {
    margin-bottom: 20px;
  }
  textarea, input[type="submit"], .nav-link {
    margin: 10px 0;
  }
  textarea {
    width: 100%;
    height: 300px;
    padding: 10px;
    font-family: monospace;
  }
  input[type="submit"] {
    padding: 10px 20px;
    font-size: 16px;
    color: white;
    background-color: #28a745;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }
  input[type="submit"]:hover {
    background-color: #218838;
  }
  .nav-link {
    display: inline-block;
    padding: 10px 20px;
    font-size: 16px;
    color: white;
    background-color: #007bff;
    border: none;
    border-radius: 4px;
    text-decoration: none;
  }
  .nav-link:hover {
    background-color: #0056b3;
  }
  select {
    width: 100%;
    padding: 10px;
  }
  .output-container {
    background-color: #2d2d2d;
    padding: 20px;
    border-radius: 5px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    color: #dcdcdc;
    font-family: monospace;
    white-space: pre-wrap;
  }
</style>
</head>
<body>
<div class="container">
  <h1>Josser's</h1>
  <form id="playbookForm" action="/run_playbook" method="post">
    <label for="playbookSelect">Selecciona playbook:</label><br>
    <select id="playbookSelect" name="playbook">
      {% for playbook in playbooks %}
      <option value="{{ playbook }}">{{ playbook }}</option>
      {% endfor %}
    </select><br><br>
    <input type="submit" value="Ejecutar playbook">
  </form>
  <a href="/playbook_factory" class="nav-link">Fábrica de Playbooks</a>
  <h2>Agregar nuevo host</h2>
  <form action="/add_host" method="post">
    <label for="hostname">Nombre del host:</label><br>
    <input type="text" id="hostname" name="hostname" required><br>
    <label for="hostip">IP del nuevo host:</label><br>
    <input type="text" id="hostip" name="hostip" required><br>
    <input type="submit" value="Agregar Host">
  </form>
  <div class="output-container" id="output">
    <pre>{{ output }}</pre>
  </div>
</div>
</body>
</html>